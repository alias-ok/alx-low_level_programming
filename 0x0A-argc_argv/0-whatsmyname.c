#include "main.h"
#include <stdio.h>
/**
 * main - Entry function that prints its name
 * @argc: argument count
 * @argv: argument vector
 * Return: Always 0 for success
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}
