#include <stdio.h>
/**
* main - c program that displays a text using printf function
*return : 0 (success when program returns 0)
*/
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
