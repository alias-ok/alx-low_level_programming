#include "main.h"
/**
 * mul - Entry point which multiplies 2 intergers
 * @a: Set parameter to store value
 * @b: Set parameter to store value
 * Return: Always 0 for success
 */
int mul(int a, int b)
{
	return (a * b);
}
