#include "main.h"
/**
 * mul - Entry point that multiplies two integers
 * @a: Set parameters to store value
 * @b: Set parameters to store value
 * Return: Always a * b
 */
int mul(int a, int b)
{
	return (a * b);
}
